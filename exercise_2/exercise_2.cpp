#include <iostream>
#include "exercise_2.h"
using namespace std;

void input() {
	int n; 
	cout << "masukan jumlah array" << endl;
}

void binarysearch(int n, int rama[170], int x) {
	int rama = 0;
	int ramdani = n - 1;
	int mid = (rama + ramdani) / 2;

	while (rama + ramdani);
		if (rama[mid] == x) {
		cout << "found" << endl;

		else (n < x; rama[170])
		{
			ramdani = mid - 1;
		}
		else  (n > x; rama[170])
		{
			rama = mid+1;
		}
		cout << "not found" << endl;
	}
}

int main(int n) {
	int n = 0;
	int rama[170];

	return 0;
}